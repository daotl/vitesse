---
import type { Article } from '../pages/articles/types'

const { data, url } = Astro.props as { data: Article; url: string }
const altInfo = 'Picture display failure';
---

<div class="pt-32px mb-32px">
  <div>
    {
      data.imageUrl && (
        <div>
          <img
            src={data.imageUrl}
            alt={altInfo}
            class="h-[25em] w-[100%]"
          />
        </div>
      )
    }
  </div>

  <div class="p-[32px] bg-[#2a2e37]">
    <h2 class="text-[32px] font-black leading-10 color-[#ebeff0]">
      <a class="text-inherit hover:underline" href={url}>{data.title}</a>
    </h2>
    <p class="mt-8px text-[14px] font-medium leading-[22px] color-[#9095a2]">
      {data.description}
    </p>

    <div>
      <div class="pt-32px mt-24px flex justify-left">
        <div class="mr-[8px]">
          <img
            src={data.authorPhotoUrl}
            alt=""
            class="rounded-[50%] h-[2em] w-[2em]"
          />
        </div>
        <div>
          <p class="text-[12px] font-600 color-[#9095a2]">
            {data.author}
          </p>
          <p class="text-[12px] leading-[18px] color-[#9095a2]">
            {data.date_created}
          </p>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  body {
    background-color: #22242b;
    h2 {
      font-family: "Monument", sans-serif;
    }
  }
</style>
